# StreamSource Live Checker Configuration
# =======================================
# This service monitors streams from StreamSource API and updates their live/offline status

# === StreamSource API Configuration (Required) ===
# The API endpoint for StreamSource
STREAMSOURCE_API_URL=https://api.streamsource.com

# Your StreamSource service account credentials (required)
# Use dedicated service account for better security
STREAMSOURCE_EMAIL=livesheet-updater@streamsource.local
STREAMSOURCE_PASSWORD=your-service-account-password

# === Rate Limiting Configuration (Optional) ===
# How often to check live streams (milliseconds)
# Default: 120000 (2 minutes)
RATE_LIVE=120000

# How often to check offline streams (milliseconds)
# Default: 420000 (7 minutes)
RATE_OFF=420000

# === Archiving Configuration (Optional) ===
# Enable automatic archiving of expired offline streams
# Default: false
ARCHIVE_ENABLED=false

# How long (in minutes) a stream must be offline before it's considered expired
# Default: 30 minutes
ARCHIVE_THRESHOLD_MINUTES=30

# How often (in milliseconds) to check for expired streams
# Only used when ARCHIVE_ENABLED=true
# Default: 300000 (5 minutes)
ARCHIVE_CHECK_INTERVAL=300000

# === Debug Configuration (Optional) ===
# Enable debug logging for troubleshooting
# Default: false
DEBUG=false