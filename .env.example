# Livesheet Checker Configuration
# ================================
# This service monitors Google Sheets for stream updates

# === Google Sheets Configuration ===
# The ID of your Google Sheet (from the URL)
# Example: https://docs.google.com/spreadsheets/d/YOUR_SHEET_ID_HERE/edit
GOOGLE_SHEET_ID=your_google_sheet_id_here

# Path to Google service account credentials JSON file
# Generate from: https://console.cloud.google.com/apis/credentials
GOOGLE_CREDENTIALS_PATH=./creds.json

# === Service Configuration ===
# How often to check streams (in milliseconds)
CHECK_INTERVAL=300000  # 5 minutes

# Maximum number of streams to check in parallel
MAX_CONCURRENT_CHECKS=10

# Timeout for each stream check (in milliseconds)
STREAM_CHECK_TIMEOUT=10000  # 10 seconds

# === Sheet Configuration ===
# Sheet name/tab to monitor (defaults to first sheet)
SHEET_NAME=Sheet1

# Column mappings (0-based index)
URL_COLUMN=0          # Column A
STATUS_COLUMN=1       # Column B
LAST_CHECKED_COLUMN=2 # Column C
TITLE_COLUMN=3        # Column D

# === StreamSource Integration (Optional) ===
# If you want to sync with StreamSource API
# STREAMSOURCE_API_URL=http://streamsource:3000/api/v1
# STREAMSOURCE_API_KEY=your_api_key_here

# === Logging ===
LOG_LEVEL=info  # debug, info, warn, error

# === Development Settings ===
# Enable verbose logging for debugging
# VERBOSE_LOGGING=true
# DRY_RUN=false  # If true, won't update the sheet